<section id="{{ section.id }}" class="{{ section.class }}">
  <div class="container">
    <h2>{{ section.title }}</h2>
    <div class="content">
      {{ section.content | safe }}
    </div>
  </div>
</section> 