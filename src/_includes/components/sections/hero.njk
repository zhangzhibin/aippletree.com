<section class="hero">
  <div class="container">
    <h1>{{ title }}</h1>
    <p class="subtitle">{{ subtitle }}</p>
    <div class="buttons">
      <a href="{{ primaryButton.url }}" class="button primary">{{ primaryButton.text }}</a>
      <a href="{{ secondaryButton.url }}" class="button secondary">{{ secondaryButton.text }}</a>
    </div>
  </div>
</section> 