{%- if title -%}
<header class="page-header">
    <h1 class="page-title">{{ title }}</h1>
    {%- if subtitle -%}
    <p class="page-subtitle">{{ subtitle }}</p>
    {%- endif -%}
</header>
{%- endif -%} 